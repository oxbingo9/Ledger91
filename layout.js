export const metadata = { title: "인사 조회", description: "년도별 의장 조회" };
export default function RootLayout({ children }) {
  return <html lang="ko"><body style={{ margin: 0 }}>{children}</body></html>;
}